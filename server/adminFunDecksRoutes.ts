import type { Express, RequestHandler } from 'express';

/** Temporary stub: Fun Decks admin routes source is missing from the workspace. */
export function registerAdminFunDecksRoutes(
  _app: Express,
  _options?: { requireAdmin?: RequestHandler },
): void {}
