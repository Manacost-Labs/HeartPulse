# Release dead reverse-forwarding sessions from the origin so managed tunnels
# can reclaim their Moscow listen ports after a network interruption.
Match User root Address 151.80.21.140
    ClientAliveInterval 30
    ClientAliveCountMax 3
