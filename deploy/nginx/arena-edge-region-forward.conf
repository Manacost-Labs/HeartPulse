# Edge-side trust boundary for regional Web Vitals.
# Include this in the canonical HTTPS server after setting
# `$arena_proxy_region`. This assignment overwrites any browser-provided value
# before the request enters the trusted origin tunnel.
proxy_set_header X-Arena-Edge-Region $arena_proxy_region;
