# Include inside every HTTP, www and legacy-host server block after installing
# arena-seo-map.conf in the nginx `http` context. Known HTML routes gain their
# canonical slash here, so scheme/host/path normalization completes in one hop.
return 301 https://hearthpulse.net$arena_canonical_edge_path$is_args$args;
